apiVersion: enonic.cloud/v1alpha1
kind: VHost
metadata:
  name: company-com
  namespace: mycloud-myproject-xp-qa
spec:
  host: company.com
  certificate:
    selfSigned: true
  mappings:
  - nodeAlias: main
    name: admin
    source: /admin
    target: /admin
    idProvider: system
