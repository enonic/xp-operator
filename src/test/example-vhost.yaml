apiVersion: enonic.cloud/v1alpha1
kind: XpVHost
metadata:
  name: company-com
  namespace: mycloud-myproject-xp-qa
spec:
  host: company.com
  certificate:
    selfSigned: true
  mappings:
    - node: main
      name: admin
      source: /admin
      target: /admin
      idProvider: system
