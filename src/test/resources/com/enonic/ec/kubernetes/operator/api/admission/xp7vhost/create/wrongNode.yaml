testResult: "XpDeployment 'mycloud-myproject-qaxp' does not contain node 'wrong'"
testReview:
  request:
    operation: CREATE
    object:
      apiVersion: enonic.cloud/v1alpha1
      kind: Xp7VHost
      metadata:
        name: test-purplejs-io
        namespace: mycloud-myproject-qaxp
      spec:
        host: test.purplejs.io
        mappings:
          - node: wrong
            source: /
            target: /admin
            idProvider: system
