testResult: "Some fields in 'spec.mappings.idProviders' are missing: [default]"
testReview:
  request:
    operation: CREATE
    object:
      apiVersion: enonic.cloud/v1alpha2
      kind: Xp7VHost
      metadata:
        name: test-purplejs-io
        namespace: mycloud-myproject-qaxp
      spec:
        host: test.purplejs.io
        mappings:
          - nodeGroup: main
            source: /
            target: /admin
            idProviders:
              enabled:
                - system
