testResult: "This host is being used by another Xp7VHost"
testReview:
  request:
    operation: CREATE
    object:
      apiVersion: enonic.cloud/v1alpha2
      kind: Xp7VHost
      metadata:
        name: test-purplejs-io
        namespace: mycloud-myproject-qaxp
      spec:
        host: taken.purplejs.io
        mappings:
          - nodeGroup: main
            source: /
            target: /admin
            idProvider: system
