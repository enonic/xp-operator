testResult: "Some fields in 'spec.mappings' are missing: [nodeGroup]"
testReview:
  request:
    operation: CREATE
    object:
      apiVersion: enonic.cloud/v1alpha2
      kind: Xp7VHost
      metadata:
        name: test-purplejs-io
        namespace: mycloud-mysolution-myenv-myservice
      spec:
        host: test.purplejs.io
        mappings:
          #- nodeGroup: main
          - source: /
            target: /admin
