testResult: ""
testReview:
  request:
    operation: CREATE
    object:
      apiVersion: enonic.cloud/v1alpha2
      kind: Xp7VHost
      metadata:
        name: healthcheck
        namespace: mycloud-mysolution-myenv-myservice
      spec:
        host: healthcheck
        options:
          ingress: false
        mappings:
          - nodeGroup: main
            source: /
            target: /admin
