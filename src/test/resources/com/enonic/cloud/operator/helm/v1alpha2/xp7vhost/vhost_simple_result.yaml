---
apiVersion: "extensions/v1beta1"
kind: "Ingress"
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/proxy-body-size: "8m"
    ingress.kubernetes.io/rewrite-target: "/"
    enonic.cloud/dns.hostname: "test.purplejs.io"
    enonic.cloud/dns.heritage: "heritage=ec-operator,id= 54edf212-742d-44f5-9fef-6749da7fd534"
    enonic.cloud/dns.cdn: "true"
    monitor.stakater.com/enabled: "true"
    monitor.stakater.com/overridePath: "/b"
    cert-manager.io/cluster-issuer: "selfsigned"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/whitelist-source-range: "1.1.1.1"
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-path: "/b"
    nginx.ingress.kubernetes.io/session-cookie-name: "54edf212-742d-44f5-9fef-6749da7fd534"
    nginx.ingress.kubernetes.io/session-cookie-change-on-failure: "true"
    nginx.ingress.kubernetes.io/affinity-mode: "balanced"
    nginx.ingress.kubernetes.io/proxy-buffering: "on"
    nginx.ingress.kubernetes.io/configuration-snippet: "# Extra config\nproxy_cache\
      \ static-cache;\nproxy_cache_use_stale error timeout updating http_404 http_500\
      \ http_502 http_503 http_504;\nproxy_cache_key $proxy_host$request_uri$cookie_jessionid;\n\
      \n# Allow to bypass cache with Pragma header\nproxy_cache_bypass $http_pragma;\n\
      \n# Send cache status header back\nadd_header X-Cache-Status $upstream_cache_status;\n"
  labels:
    enonic.managed: "true"
  name: "test-purplejs-io-001af4f0a7"
  namespace: "mycloud-mysolution-myenv-myservice"
spec:
  rules:
  - host: "test.purplejs.io"
    http:
      paths:
      - backend:
          serviceName: "frontend"
          servicePort: 8080
        path: "/b"
  tls:
  - hosts:
    - "test.purplejs.io"
    secretName: "54edf212-742d-44f5-9fef-6749da7fd534"
---
apiVersion: "extensions/v1beta1"
kind: "Ingress"
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    ingress.kubernetes.io/rewrite-target: "/"
    enonic.cloud/dns.hostname: "test.purplejs.io"
    enonic.cloud/dns.heritage: "heritage=ec-operator,id= 54edf212-742d-44f5-9fef-6749da7fd534"
    enonic.cloud/dns.cdn: "true"
    cert-manager.io/cluster-issuer: "selfsigned"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/configuration-snippet: "# Extra config\n"
  labels:
    enonic.managed: "true"
  name: "test-purplejs-io-a6ac1f2c27"
  namespace: "mycloud-mysolution-myenv-myservice"
spec:
  rules:
  - host: "test.purplejs.io"
    http:
      paths:
      - backend:
          serviceName: "all"
          servicePort: 8080
        path: "/a"
  tls:
  - hosts:
    - "test.purplejs.io"
    secretName: "54edf212-742d-44f5-9fef-6749da7fd534"
